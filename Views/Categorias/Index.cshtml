@model IEnumerable<FocinhosFelizes1.Models.Categoria>

@{
    ViewData["Title"] = "Categorias";
}

<div id="listaCategorias" class="container tabela-reservas text-dark p-5 my-5 mt-6 d-flex flex-column justify-content-center" style="min-height: 60vh;">
    <h1 class="text-center text-dark mb-3">Categorias</h1>

    <div class="row align-items-start gap-3 mb-4">
        <!-- Botão Adicionar -->
        <div class="col-auto d-flex align-items-stretch">
            <a asp-action="Create" class="btn btn-secondary px-4 d-flex align-items-center" style="height: 54px;">
                <i class="bi bi-plus-circle me-2"></i> Adicionar
            </a>
        </div>

        <!-- Campo de Busca -->
        <div id="divBuscarCategoria" class="col">
            <form asp-action="Search" method="get">
                <div class="input-group">
                    <input type="text" name="searchTerm" value="@Context.Request.Query["searchTerm"]" class="form-control" placeholder="Buscar por tipo ou descrição..." style="height: 54px;" />
                    <button type="submit" class="btn btn-secondary">
                        <i class="bi bi-search"></i> Buscar
                    </button>
                </div>
            </form>
        </div>
    </div>

    <table class="table tabela-reservas text-dark table-striped">
        <thead>
            <tr>
                <th>Tipo</th>
                <th>Descrição</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.TipoCategoria</td>
                    <td>@item.DescricaoCategoria</td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@item.CategoriaId" class="text-dark me-2">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.CategoriaId" class="text-dark me-2">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
