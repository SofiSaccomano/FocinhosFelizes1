@model IEnumerable<FocinhosFelizes1.Models.Estoque>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produtos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QtdEstoque)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Produtos.ProdutosId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtdEstoque)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstoqueId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstoqueId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstoqueId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
